# Frontend environment for Docker
# NOTE: For Docker, env vars should NOT have quotes

# Backend API URL (for local Docker testing, use host.docker.internal)
# For production/Railway: use the internal service URL
NEXT_PUBLIC_API_URL=http://localhost:3000

# App metadata
NEXT_PUBLIC_APP_NAME=UAMS
NEXT_PUBLIC_ENV=development
